{/*"use client";

import type { Role } from "@/types/user";

type Props = {
    userId: number;
    role: Role;
};

export default function ProfileDashboardCards({
    userId,
    role,
}: Props) {
    return (
        <>
            {role === "EXPOSANT" ? (
                <ExposantDashboard userId={userId} />
            ) : (
                <OrganisateurDashboard userId={userId} />
            )}
        </>
    );
}*/}